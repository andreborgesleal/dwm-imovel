@using DWM.Models.Repositories;
@model System.Web.Mvc.PagedList<EmpreendimentoViewModel>
@{
    Layout = null;
}

@if (Model != null)
{
    <div class="row Scroll" style="margin-top: -15px">
        <div class="col-md-12">
            <table class="table table-hover table-bordered table-condensed" style="cursor: pointer">
                <caption></caption>
                <thead class="corvo text-enfatico">
                    <tr>
                        <th>Nome</th>
                    </tr>
                </thead>
                @foreach (EmpreendimentoViewModel emp in Model)
                {
                    <tr onclick="Move('@emp.empreendimentoId', '@emp.nomeEmpreend')">
                        <td class="col-md-7 col-sm-2">
                            @emp.nomeEmpreend
                        </td>
                    </tr>
                }
            </table>
        </div>
    </div>
    @Html.Partial("Paginacao", Model)
}
else
{
    @Html.Partial("_AcessoNegadoModal")
}
